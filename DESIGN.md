# 设计文档：Claude Multi-Agent Workflows

本文解释这套多 agent 工作流**为什么这样编排**，以及**如何迁移到你自己的环境**。不涉及具体命令用法（见 README）。

## 一、要解决的问题

多 agent 协作常见的失败原因，往往不是"模型不会写代码"，而是协作机制崩了：

| 痛点 | 后果 |
|---|---|
| 任务状态散落在聊天记录里 | 跨会话、换人就丢上下文 |
| 指挥官与执行者靠"口头"传上下文 | 指令漂移，执行者各干各的 |
| 执行者声称"完成了" | 没有任何可核对的证据 |
| 多个执行者同时改文件 | 越界改动、互相覆盖 |

这套工作流的全部设计，都是围绕"**让协作状态可核对、可交接**"展开。

## 二、四个核心设计决策

### 1. 黑板模式（TASKS.md）
所有任务状态集中在**唯一一个文件**：owner / 状态 / 产出 / 阻塞。谁都能读、能更新。聊天只传状态和路径，正文全进文件。

```
| Task | Owner | Status | 规则 / 产出 | Blockers |
|---|---|---|---|---|
| `001` | codex | DONE | `artifacts/rules/001-x.md` | None |
```

### 2. 规则即唯一事实源
每个任务一份**自包含规则文件**，含：目标 / 范围 / 排除 / 约束 / 验收标准 / 输出证据。执行者只看规则就能独立干活，**不依赖指挥官对话上下文**。

> 一个规则就是一份"任务契约"：它是规则集，执行者必须逐条遵守，任何一条未满足即未完成。

### 3. 职责分离
- **指挥官**（Claude）：拆解、写规则、派发、审查、验收、交接
- **执行者**（codex/zcode/Claude 子代理）：按规则实现，产出代码与证据
- **审查者**（Claude 独立只读）：挑错，CRITICAL/HIGH 阻断
- **验证者**（Claude）：核对"确实跑了"的证据

关键：**审查必须发生在派发之外的另一轮思考**，不能"写完自己盖章"。

### 4. 薄编排层
执行交给模型最强的工具，编排层（`caw`）只做流程控制：建规则 → 派发 → 审查 → 验收 → 交接。它不替模型干活，所以代码很薄（一个纯标准库文件）。

## 三、组件与生命周期

```
① 拆解   caw new-task     生成规则 + 登记黑板
② 派发   caw dispatch     按 owner 路由出执行命令
③ 执行   codex/zcode/子代理 按规则实现，产出到 artifacts/code/
④ 审查   caw review --report  审查报告落盘 artifacts/review/
⑤ 验收   caw verify --evidence 证据写入规则，置 DONE
⑥ 交接   caw handoff       跨会话交接文档
```

一个任务的完整状态流：
`BACKLOG → IN_PROGRESS → REVIEW → DONE`（或 `BLOCKED` 暂停）。

## 四、如何迁移到你自己的栈

这套协议是**文件级的**，工具只是加速器。迁移 = 改三处：

### 1. 换执行者
打开 `caw.py` 顶部的 `DISPATCH` 映射，改成你的 CLI：

```python
DISPATCH = {
    "my-cli": 'my-cli "读 {rule} 并严格执行"',
    "claude-subagent": "用 Claude 子代理执行：{rule}",
}
```

不换工具也能跑：协议本身只要"读规则 → 实现 → 回报证据"，任何能读文件的 agent 都能当执行者。

### 2. 换语言 / 环境
`caw.py` 纯标准库，`python3 caw.py` 即可。如果你想换语言重写，协议就是几个文件约定：
- `TASKS.md` 一张表格
- `artifacts/rules/*.md` 按模板填
- `artifacts/review|verify|handoff/` 存报告

重写成本很低，因为核心是**文件约定**，不是代码。

### 3. 裁剪 / 扩展
- 不需要的 skill 直接删掉对应目录
- 想加状态（如 `WAITING`）改 `STATES` 元组
- 想加执行者，加一行 `DISPATCH` + 一个 owner 名

## 五、设计权衡

| 选择 | 权衡 |
|---|---|
| 规则文件 vs 数据库存状态 | 文件：零依赖、可 diff、可人工改；代价是并发时需注意 |
| 外部执行者 vs 内置 agent | 外部：复用模型最强能力；代价是需各自配置认证 |
| 薄编排 vs 厚平台 | 薄：好维护、好迁移；代价是高级能力（插件、双运行时同步）需要自己加 |

## 六、适用边界

**适合**：长期项目、跨会话协作、多 CLI 分工、需要验收可追溯。
**不适合**：一次性小任务（直接让一个 agent 干完更快）、强实时协作、需要复杂权限控制的企业场景。

---

配套：`README.md`（用法）、`skills/commander-executor/SKILL.md`（协议全文）、`examples/commander-executor/`（最小示例）。
